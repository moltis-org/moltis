fragment SessionFields on SessionEntry {
  id
  key
  label
  model
  createdAt
  updatedAt
  messageCount
  archived
}

query FetchSessions {
  sessions {
    list {
      ...SessionFields
    }
  }
}

query SearchSessions($query: String!) {
  sessions {
    search(query: $query) {
      ...SessionFields
    }
  }
}
